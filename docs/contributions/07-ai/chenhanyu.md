# AI 功能集成贡献说明

姓名：chenhanyu  
学号：2320100624  
日期：2026-04-21

## 我完成的工作

### 1. AI 功能

- 功能类型：智能客服 / 宠物健康问答
- 使用模型：DeepSeek Chat（OpenAI 兼容接口）

### 2. 实现内容

- [x] 后端 API
- [x] 前端调用对齐
- [x] 错误处理

补充说明：

- 新增 `POST /api/v1/ai/chat`，按项目统一结构返回 `{ code, message, data }`
- 新增 `ai.*` 配置，支持通过 `AI_API_KEY` / `DEEPSEEK_API_KEY`、`AI_BASE_URL`、`AI_MODEL` 管理模型接入
- 封装 AI 调用服务，追加系统提示词并保留最近对话上下文
- 增加后端 WebMvc 测试，覆盖匿名访问、参数校验和未配置密钥时的错误返回
- 同步更新 `docs/api.yaml`、`docs/frontend-api.md`、`docs/ai-feature.md`

## PR 链接

- 待创建 / 合并后补充

## 心得体会

这次集成让我把“能调用大模型”和“能把大模型能力接进现有项目”区分得更清楚了。课程作业里真正重要的不只是拿到一段回复，而是把配置管理、统一返回、错误处理、前后端接口约定和测试一起补齐。基于现有 Spring Boot 分层结构做最小改动，比额外引入复杂 AI 基础设施更适合当前项目阶段，也更便于团队联调和答辩演示。
